package com.example.ratemapapp.map.provider

import com.yandex.mapkit.images.ImageDataDescriptor
import com.yandex.mapkit.images.ImageUrlProvider

class ImageUrlProviderImpl : ImageUrlProvider {
    override fun formatUrl(p0: ImageDataDescriptor): String {
        return ""
    }
}